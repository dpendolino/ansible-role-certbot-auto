---
- hosts: localhost
  remote_user: root
  roles:
    - ansible-role-certbot-auto
  tasks:
    - name: Assert that certbot-auto runs
      shell: /usr/local/bin/certbot-auto --help
      failed_when: result.rc > 1
